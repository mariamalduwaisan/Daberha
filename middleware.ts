// Middleware intentionally left empty.
// Auth protection is handled in app/dashboard/layout.tsx (server component).
// Sign-in redirect for logged-in users is handled in app/page.tsx.
export const config = { matcher: [] };
